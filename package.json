{

  "scripts": {
    "build": "rollup -c && javascript-obfuscator dist/fingerprint.min.js --output dist/fingerprint.min.js"
  },

  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^2.79.2"
  }
}
