## Setup

1. Install PHP dependencies:
    ```bash
    composer install
    ```

2. Install Node.js dependencies:
    ```bash
    npm install
    ```

3. Add a `.env` file with your environment variables.

## Building Fingerprint JavaScript

To apply changes to the fingerprint JavaScript, rebuild the project using Rollup:

```bash
npx run build
```
